yri_sd_mealy_automaton_spec YERITH_QVGE_sample_SAFETY_PROPERY_one
{
 START_STATE(CLOSED):IN_PRE(G, alias_set.F)
  -> [in_sql_event_log('DELETE.file.F', START_STATE(CLOSED))] / 'UPDATE.file.G'->
   FINAL_STATE(WRITE_AFTER_CLOSE).
}

